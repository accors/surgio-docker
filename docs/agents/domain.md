# Domain Docs

Before exploring this repository, read relevant root `CONTEXT.md` and ADRs in `docs/adr/` when they exist. If absent, proceed silently.

This is a single-context repository:

```
/
|- CONTEXT.md
`- docs/adr/
```

Use terminology defined in `CONTEXT.md`. Surface conflicts with existing ADRs instead of silently overriding them.
